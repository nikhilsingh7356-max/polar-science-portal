'use client';import {useState} from 'react';import {useRouter} from 'next/navigation';import Link from 'next/link';import {api} from '../../lib/api';
export default function Login(){const[email,setEmail]=useState('admin@polar.local'),[password,setPassword]=useState('Admin@123'),[err,setErr]=useState('');const router=useRouter();async function submit(e){e.preventDefault();try{const x=await api('/api/auth/login',{method:'POST',body:JSON.stringify({email,password})});localStorage.setItem('token',x.access_token);localStorage.setItem('user',JSON.stringify(x.user));router.push('/admin')}catch(e){setErr(e.message)}}return <main className="container"><form className="form card" onSubmit={submit}><Link className="brand" href="/">POLAR<span>INDIA</span></Link><h1>Sign in</h1><p className="muted">Admin/researcher demo access</p><input className="input" value={email} onChange={e=>setEmail(e.target.value)} placeholder="Email"/><br/><br/><input className="input" type="password" value={password} onChange={e=>setPassword(e.target.value)} placeholder="Password"/><br/><br/><button className="btn primary" style={{width:'100%'}}>Sign in</button>{err&&<p>{err}</p>}<p className="muted">Demo admin: admin@polar.local / Admin@123</p></form></main>}
