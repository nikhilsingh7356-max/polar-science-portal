'use client';import {useEffect,useState} from 'react';import Link from 'next/link';import dynamic from 'next/dynamic';import {api} from '../../lib/api';
const Map=dynamic(()=>import('../../components/PolarMap'),{ssr:false});
export default function MapPage(){const[x,setX]=useState([]);useEffect(()=>{api('/api/expeditions').then(setX)},[]);return <main className="container section"><div style={{display:'flex',justifyContent:'space-between'}}><div><div className="eyebrow">GIS exploration</div><h1>Polar expedition map</h1></div><Link className="btn" href="/">Home</Link></div><Map expeditions={x}/><div className="grid" style={{marginTop:18}}>{x.map(e=><div className="card" key={e.id}><span className="tag">{e.region}</span><h3>{e.name}</h3><p className="muted">{e.year} • {e.description}</p></div>)}</div></main>}
