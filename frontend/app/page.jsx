'use client';
import Link from 'next/link';
import {useEffect,useState} from 'react';
import {api} from '../lib/api';
export default function Home(){const [exps,setExps]=useState([]); const [resources,setResources]=useState([]); useEffect(()=>{Promise.all([api('/api/expeditions'),api('/api/resources')]).then(([a,b])=>{setExps(a);setResources(b)})},[]); return <><nav className="nav"><div className="container" style={{display:'flex',justifyContent:'space-between',width:'100%'}}><Link className="brand" href="/">POLAR<span>INDIA</span></Link><div className="navlinks"><Link href="/explore">Explore</Link><Link href="/map">Expedition Map</Link><Link href="/login" className="btn">Admin / Researcher</Link></div></div></nav><main><section className="hero"><div className="container"><div className="eyebrow">Integrated Polar Science Outreach • SIH 26063</div><h1>One gateway to India’s polar knowledge.</h1><p>Explore expeditions, reports, datasets, publications and media in a connected knowledge repository designed for researchers, students and the public.</p><div style={{display:'flex',gap:12,marginTop:26}}><Link className="btn primary" href="/explore">Explore Knowledge</Link><Link className="btn" href="/map">View Expeditions</Link></div><div className="stats"><div className="stat"><strong>{exps.length}</strong><span className="muted">Expeditions</span></div><div className="stat"><strong>{resources.length}</strong><span className="muted">Knowledge Resources</span></div><div className="stat"><strong>AI</strong><span className="muted">Outreach drafts</span></div></div></div></section><section className="section"><div className="container"><div className="eyebrow">Knowledge repository</div><h2>Discover polar science</h2><div className="grid">{resources.slice(0,3).map(r=><Link className="card resource" key={r.id} href={`/resources/${r.id}`}><span className="tag">{r.resource_type}</span><h3>{r.title}</h3><p className="muted">{r.description}</p><small>{r.year||'—'} • {r.author||'Institutional resource'}</small></Link>)}</div></div></section></main><footer className="footer"><div className="container">Prototype for SIH 2026 PS 26063 • Sample data is clearly intended for demonstration.</div></footer></>}
